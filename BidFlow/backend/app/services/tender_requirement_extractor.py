# 负责人：成员 B
#
# 你要做什么：把解析出的招标文本转换为可处理的资格、商务和技术响应项。
#
# 开始前确认：document_parser.py 已返回文本和来源位置；D 已确认 llm_client 的调用方式或先使用固定 Mock 结果。
#
# 实现顺序：
# 1. 将长文本按标题、段落或页码分组，避免一次发送过长内容。
# 2. 让模型按固定 JSON 格式返回 category、content、source_text、source_ref。
# 3. 校验返回内容；类别不合法时归为 other，不直接写入错误数据。
# 4. 按规则分配优先级：资格、截止、必交材料为 P0；技术和商务为 P1；其他为 P2。
# 5. 写入 requirements 表，并保存对应 tender_document_id。
#
# 完成后手动验证：用模拟招标书解析，确认至少有资格、技术、商务三类，且每条都有原文来源。
