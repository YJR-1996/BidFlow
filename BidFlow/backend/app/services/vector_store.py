# 负责人：成员 C
#
# 你要做什么：把文本块转换为向量并保存到 Chroma，同时支持按条件检索和删除。
# 实现顺序：1）从 config.py 读取 Chroma 目录与 Embedding 配置；2）初始化持久化集合；3）批量生成向量，避免一次发送过多文本；4）写入文本、向量和元数据；5）检索时按 owner_id 或 project_id 过滤；6）按 document_id 删除所有相关块。
# 出错时：Embedding 失败不得把资料标为 success；删除失败要阻止数据库记录提前删除。
# 完成后验证：写入后可用关键词召回；删除资料后查询次数为零。
