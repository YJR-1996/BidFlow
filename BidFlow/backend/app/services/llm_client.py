# 负责人：成员 D
#
# 你要做什么：将大模型供应商调用封装为一个全组可复用的服务。
# 实现步骤：1）从 config.py 获取 API Key、模型名和超时；2）实现发送 messages 的统一函数；3）限制重试次数；4）超时或接口失败时转换为业务异常；5）记录调用耗时与错误类型，绝不记录密钥和完整敏感材料。
# 完成后验证：将 API Key 配错时，接口返回清晰服务错误而非 Python 堆栈；其他服务不直接 import 厂商 SDK。
