# 负责人：组长／成员 A
#
# 你要做什么：证明认证模块在正常和异常情况下都安全工作。
#
# 编写顺序：
# 1. 测试注册新用户返回成功且数据库没有明文密码。
# 2. 测试重复用户名返回业务错误。
# 3. 测试正确用户名密码返回 Token。
# 4. 测试错误密码不能得到 Token。
# 5. 测试无 Token 和伪造 Token 请求受保护接口均返回 401。
#
# 完成后验证：运行 pytest tests/test_auth.py -v，全部通过。
