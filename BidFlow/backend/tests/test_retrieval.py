# 负责人：成员 C
#
# 你要做什么：证明资料处理、向量检索和删除不会泄露或残留数据。
# 编写顺序：1）测试切分后的块有 document_id 和来源位置；2）测试写入后能被查询命中；3）测试用户／项目隔离；4）测试删除资料后无结果；5）测试空查询被拒绝。
# 完成后验证：运行 pytest tests/test_retrieval.py -v，测试不连接真实向量库或真实 Embedding 服务。
