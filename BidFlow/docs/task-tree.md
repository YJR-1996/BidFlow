<!--
负责人：全组共同维护，组长审核。

你要做什么：把每个人的工作拆成可勾选的小步骤，避免四个人同时改同一文件或等别人完成后才发现接口不一致。

每次开始一个文件前：
1. 在飞书／腾讯表格把该任务状态改为“开发中”。
2. 确认本文件负责人、依赖文件和验收方法。
3. 如果依赖文件还没完成，先写接口约定，不要猜字段名。

每次完成一个文件后：
1. 运行该模块的手动验证步骤。
2. 在表格登记接口路径、请求字段和返回字段。
3. 提交 Git，提交信息以 feat:、fix: 或 docs: 开头。
4. 请另一位组员检查后再合并到 develop。

任务树：
1. 组长先完成配置、数据库、认证和启动。
2. 成员 B 再完成项目、文件上传、文本解析和需求清单。
3. 成员 C 在企业资料上传完成后，实现切分、向量化和检索。
4. 成员 D 只在 B 的需求接口与 C 的检索函数确定后，实现草稿、规则和报告。
5. 前端在接口字段稳定后接入真实数据；此前使用 Mock 数据搭页面。

验收：每个任务必须有负责人、依赖、接口或输出、手动验证结果和一次 Git 提交记录。
-->
